{
    "imports": [
      "$import torch",
      "$from monai.networks.nets import UNet"
    ],
  
    "device": "$torch.device('cuda' if torch.cuda.is_available() else 'cpu')",
  
    "network_def": {
      "_target_": "UNet",
      "spatial_dims": 3,
      "in_channels": 1,
      "out_channels": 1,
      "channels": [16, 32, 64],
      "strides": [2, 2],
      "num_res_units": 2
    },
  
    "network": "$@network_def.to(@device)",
  
    "preprocessing": {
      "description": "Input CBCT volume should be resized or padded to shape [1, 64, 64, 32] and intensity-normalized before inference."
    },
  
    "inferer": {
      "_target_": "SlidingWindowInferer",
      "roi_size": [64, 64, 32],
      "sw_batch_size": 1,
      "overlap": 0.25
    },
  
    "postprocessing": {
      "description": "Apply sigmoid activation followed by thresholding at 0.5 to produce binary segmentation mask."
    },
  
    "output": {
      "type": "binary_segmentation_mask",
      "shape": [1, 64, 64, 32]
    },
  
    "note": "This is an initial research-prototype inference configuration. A trained model weight file must be added under models/model.pt before real inference."
  }